{
	"name": "kotobad",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest",
		"daisyui": "^5.0.54",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.4",
		"wrangler": "^4.42.2"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "bun --filter '*' dev",
		"dev:frontend": "bun --filter 'frontend' dev",
		"dev:backend": "bun --filter 'backend' dev",
		"build:frontend": "bun --filter 'frontend' next build",
		"build:backend": "bun --filter 'backend' next build",
		"deploy:frontend": "bun --filter '@kotobad/frontend' deploy",
		"deploy:backend": "bun --filter '@kotobad/backend' deploy",
		"format": "biome format --write",
		"lint": "biome lint --write",
		"check": "biome check --write",
		"prepare": "husky"
	},
	"lint-staged": {
		"packages/**/*.{ts,tsx,json}": [
			"bunx biome format --write",
			"bunx biome check --write"
		]
	},
	"dependencies": {
		"@biomejs/biome": "^2.2.2"
	}
}
